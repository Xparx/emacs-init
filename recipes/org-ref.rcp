(:name org-ref
       :description "Org-ref - The best reference handling for org-mode"
       :website "https://github.com/jkitchin/org-ref"
       :type github
       :pkgname "jkitchin/org-ref"
       :depends (dash s f ebib helm-bibtex hydra key-chord)
       ;; :features "org-ref"
       ;; :prepare '(("make"))
       :build `(("make" ,(format "EMACS=%s" el-get-emacs))))
